name: 'nusantara_context_ab_lab'
version: '1.0.0'
config-version: 2

# Profile name matching profiles.yml configuration
profile: 'nusantara_context_ab_lab'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  nusantara_context_ab_lab:
    # Staging models are views
    staging:
      +materialized: view
      +schema: staging
    # Marts models are loaded as physical tables
    marts:
      +materialized: table
      +schema: marts
